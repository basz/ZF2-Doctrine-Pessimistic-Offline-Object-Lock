<?php

return [
    'hf_pool'         => [
        /**
         * Time to life for acquired lock from now in seconds, defaults to 15 minutes
         */
        // 'ttl'                 => 900,

        /**
         * Doctrine connection the manager should use
         */
        // 'doctrine_connection' => 'doctrine.connection.orm_default',
    ],
];